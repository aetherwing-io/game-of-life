# Core experiment (LLM-as-cellular-automaton)
torch>=2.2          # MPS backend ships with the standard macOS wheel (M-series)
transformers>=4.40   # newer models need newer releases: gemma-4 (gemma4_unified) needs >=5.6
numpy>=1.24
matplotlib>=3.7
pillow>=10.0        # animated-GIF writer

# The `reference` subcommand (known-class CA baselines) needs only numpy +
# matplotlib + pillow — torch/transformers are required only for the LLM runs.

# Optional: only for `--arch mlx` (Apple-Silicon-native quantized checkpoints,
# e.g. 2-bit ternary). Not needed for any torch/transformers run.
# mlx-lm>=0.31      # Apple Silicon only
